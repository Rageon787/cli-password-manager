
fn generate_password () {
    // generate a password from a set of options 
    //      alpha, numeric, alpha-numeric 
    //      special characters (y / n) 
    //      Capitalization if (alpha / alphanumeric)  
    //      Length of password  
} 

fn display_vault() {
    // display the vault or a sub-vault
}

fn main() {
    println!("Hello, world!");
}
